Apache NiFi is used to orchestrate the data streams.

The configured flow needs to be updated to read from Elasticsearch instead of a file.

This is a diagram of the current flow.

.. image:: ../../images/nifi_flow.png
